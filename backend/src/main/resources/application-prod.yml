
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/manitodb
    username: manito
    password: manito
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: none
      properties:
        hibernate:
          show_sql: true
          format_sql: true

  logging.level:
    org.hibernate.SQL: debug